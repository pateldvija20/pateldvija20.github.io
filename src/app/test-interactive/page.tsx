import { HomeInteractive } from "@/components/HomeInteractive";

export default function TestInteractivePage() {
  return (
    <main style={{ width: "100vw", height: "100vh", overflow: "hidden", background: "#f3f2ee" }}>
      <HomeInteractive />
    </main>
  );
}
